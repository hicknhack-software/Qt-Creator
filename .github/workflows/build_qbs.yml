name: Qbs Build Matrix

on: [push, pull_request]

env:
  LIBGIT2_VERSION: 1.3.0

jobs:
  build:
    name: ${{ matrix.config.name }}
    runs-on: ${{ matrix.config.os }}
    strategy:
      fail-fast: false
      matrix:
        config:
          - name: "Windows 2022 MSVC"
            artifact: "Windows-MSVC"
            os: windows-2022
            qt_version: "6.3.0"
            qt_arch: "win64_msvc2019_64"
            qt_modules: qtbase qtdeclarative qttools qtsvg qttranslations qtimageformats qtserialport qtquicktimeline qtquick3d qt5compat qtshadertools
            qt_tools: tools_openssl_x64,1.1.1-10,qt.tools.openssl.win_x64
            qt_version_x86: "5.15.2"
            qt_arch_x86: "win32_msvc2019"
            qt_modules_x86: qtbase qtdeclarative qttools qtsvg qtserialport qtquickcontrols qtquickcontrols2 qtgraphicaleffects qtlocation qtimageformats
            libclang_url: "https://download.qt.io/development_releases/prebuilt/libclang/libclang-release_14.0.3-based-windows-vs2019_64.7z"
            jom_url: "https://download.qt.io/official_releases/jom/jom_1_1_3.zip"
            qbs_url: "https://download.qt.io/official_releases/qbs/1.22.1/qbs-windows-x86_64-1.22.1.zip"
            vcvars_x64: C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat
            vcvars_x86: C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars32.bat

    steps:
      - name: Get LibClang
        shell: cmake -P {0}
        run: file(DOWNLOAD "${{ matrix.config.libclang_url }}" ./libclang.7z SHOW_PROGRESS)

      - run: 7z x libclang.7z

      - uses: actions/setup-python@v2
        with:
          python-version: "3.x"

      - name: Get Qbs
        shell: cmake -P {0}
        run: file(DOWNLOAD "${{ matrix.config.qbs_url }}" ./qbs.zip)

      - run: |
          7z x qbs.zip -oqbs
          echo "${{ github.workspace }}/qbs/bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Get JOM
        shell: cmake -P {0}
        run: file(DOWNLOAD "${{ matrix.config.jom_url }}" ./jom.zip)

      - name: Generate Cache Keys
        run: |
          echo "${{ matrix.config.qt_arch }}" >> cache-qt.txt
          echo "${{ matrix.config.qt_modules }}" >> cache-qt.txt
          echo "${{ matrix.config.qt_tools }}" >> cache-qt.txt
          echo "${{ matrix.config.qt_arch_x86 }}" >> cache-qt-x86.txt
          echo "${{ matrix.config.qt_modules_x86 }}" >> cache-qt-x86.txt

      - name: Cache Qt
        id: cache-qt
        uses: actions/cache@v1
        with:
          path: ${{ runner.workspace }}/x64/Qt/
          key: CacheQtx64-${{ matrix.config.qt_version }}-${{ hashFiles('cache-qt.txt') }}

      - name: Install Qt
        uses: jurplel/install-qt-action@v2
        with:
          dir: ${{ runner.workspace }}/x64/
          version: "${{ matrix.config.qt_version }}"
          arch: "${{ matrix.config.qt_arch }}"
          modules: ${{ matrix.config.qt_modules }}
          tools: ${{ matrix.config.qt_tools }}
          cached: ${{ steps.cache-qt.outputs.cache-hit }}

      - name: Copy OpenSSL DLLs
        run: |
          copy ${{ runner.workspace }}/x64/Qt/Tools/OpenSSL/Win_x64/bin/*.dll $Env:Qt6_DIR\bin

      - name: Download libgit2
        id: libgit2Download
        shell: cmake -P {0}
        run: |
          set(libgit2_version "$ENV{LIBGIT2_VERSION}")
          set(libgit2_url "https://github.com/libgit2/libgit2/archive/refs/tags/v${libgit2_version}.zip")
          file(DOWNLOAD "${libgit2_url}" ./v${libgit2_version}.zip SHOW_PROGRESS)
          file(MAKE_DIRECTORY libgit2Src)
          execute_process(COMMAND ${CMAKE_COMMAND} -E tar xf ../v${libgit2_version}.zip WORKING_DIRECTORY libgit2Src)
          file(TO_CMAKE_PATH "$ENV{GITHUB_WORKSPACE}/libgit2Src/libgit2-${libgit2_version}" libgit2_src_dir)
          message("::set-output name=libgit2_src_dir::${libgit2_src_dir}")

      - name: Build libgit2
        id: libgit2Build
        run: |
          cmake -B libgit2-build -DUSE_SSH=OFF -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=libgit2-install -DBUILD_CLAR=OFF "${{ steps.libgit2Download.outputs.libgit2_src_dir }}"
          cmake --build libgit2-build --target install

      - name: Install libgit2
        id: libgit2Install
        shell: cmake -P {0}
        run: |
          file(TO_CMAKE_PATH "$ENV{GITHUB_WORKSPACE}/libgit2-install" libgit2_install)
          message("::set-output name=libgit2_install_dir::${libgit2_install}")

      - name: Setup Qbs
        run: |
          qbs setup-toolchains --type msvc $(cmd /c '"${{ matrix.config.vcvars_x64 }}" >nul & where cl') msvc-x64
          qbs setup-qt $Env:Qt6_DIR\bin\qmake.exe qt-x64
          qbs config profiles.qt-x64.baseProfile msvc-x64

      - uses: actions/checkout@v2
        with:
          path: Repo

      - working-directory: Repo
        run: |
          git submodule set-url -- perfparser https://code.qt.io/qt-creator/perfparser.git
          git submodule set-url -- qbs https://github.com/arBmind/qbs.git
          git submodule update --init --recursive

      - name: Debug LibGit2
        run: |
            echo "::debug::Set Path is ${{ steps.libgit2Install.outputs.libgit2_install_dir }}"

      - name: Build
        working-directory: Repo
        run: |
          $Env:Path += ";${{ github.workspace }}\libclang\bin"
          $Env:LLVM_INSTALL_DIR = "${{ github.workspace }}\libclang"
          $Env:QTC_ENABLE_CLANG_LIBTOOLING = "true"
          $Env:PYTHON_INSTALL_DIR = "$Env:pythonLocation"
          qbs build -d $ENV:TMP\build `
            qbs.installRoot:${{ github.workspace }}/install-root `
            modules.libgit2.rootPath:${{ steps.libgit2Install.outputs.libgit2_install_dir }} `
            profile:qt-x64 config:Release qbs.defaultBuildVariant:release

      - name: Deploy
        working-directory: ${{ github.workspace }}/install-root
        run: |
          python -u ${{ github.workspace }}/Repo/scripts/deployqt.py -i --llvm-path ${{ github.workspace }}/libclang --libgit2-path ${{ steps.libgit2Install.outputs.libgit2_install_dir }} ${{ github.workspace }}/install-root/bin/qtcreator.exe $Env:Qt6_DIR/bin/qmake.exe

      - name: x86 Cache Qt
        id: cache-qt-x86
        uses: actions/cache@v1
        with:
          path: ${{ runner.workspace }}/x86/Qt/
          key: CacheQtx86-${{ matrix.config.qt_version_x86 }}-${{ hashFiles('cache-qt-x86.txt') }}

      - name: x86 Install Qt
        uses: jurplel/install-qt-action@v2
        with:
          dir: ${{ runner.workspace }}/x86/
          version: "${{ matrix.config.qt_version_x86 }}"
          arch: ${{ matrix.config.qt_arch_x86 }}
          modules: ${{ matrix.config.qt_modules_x86 }}
          cached: ${{ steps.cache-qt-x86.outputs.cache-hit }}

      - name: x86 Setup Qbs
        run: |
          qbs setup-toolchains --type msvc $(cmd /c '"${{ matrix.config.vcvars_x86 }}" >nul & where cl') msvc-x86
          qbs setup-qt $Env:Qt5_DIR\bin\qmake.exe qt-x86
          qbs config profiles.qt-x86.baseProfile msvc-x86

      - name: x86 Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.x"
          architecture: 'x86'

      - name: x86 Build
        working-directory: Repo
        run: |
          $Env:PYTHON_INSTALL_DIR = "$Env:pythonLocation"
          qbs build -d $ENV:TMP\build-x86 `
            qbs.installRoot:${{ github.workspace }}/install-root `
            profile:qt-x86 config:Release qbs.defaultBuildVariant:release `
            products.qtcreator.condition:false projects.Plugins.condition:false projects.Tools.condition:false `
            -p qtcreatorcdbext

      - name: Add JOM
        run: 7z x jom.zip -o${{ github.workspace }}/install-root/bin/jom

      - name: Pack
        working-directory: ${{ github.workspace }}/install-root
        run: 7z a ../QtCreator-${{ matrix.config.qt_arch }}-${{ github.run_id }}.7z * -r

      - name: Upload
        uses: actions/upload-artifact@v2
        with:
          path: ${{ github.workspace }}/QtCreator-${{ matrix.config.qt_arch }}-${{ github.run_id }}.7z
          name: QtCreator-${{ matrix.config.qt_arch }}-${{ github.run_id }}.7z

      - name: Upload binaries to release
        if: contains(github.ref, 'tags/v')
        uses: svenstaro/upload-release-action@v2
        with:
          tag: ${{ github.ref }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          release_name: Release ${{ github.ref }}
          # body: ${{ github.event.head_commit.message }}
          overwrite: true
          file: ${{ github.workspace }}/QtCreator-${{ matrix.config.qt_arch }}-${{ github.run_id }}.7z
